# AboutMe Vue Project

This is a simple Vue 3 project scaffolded with Vite and Vue Router.

## Available Scripts

- `npm install` - install dependencies
- `npm run dev` - start development server
- `npm run build` - build for production
- `npm run serve` - preview production build

The router includes three routes: Home (`/`), Portfolio (`/portfolio`), and Experience (`/experience`).